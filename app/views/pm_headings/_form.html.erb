<%= form_for(@pm_heading) do |f| %>
  <% if @pm_heading.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pm_heading.errors.count, "error") %> prohibited this pm_heading from being saved:</h2>

      <ul>
      <% @pm_heading.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :heading %><br>
    <%= f.text_field :heading ,class: "form-control"%>
  </div>
  <div class="actions">
    <%= f.submit class: "btn btn-success"%>
  </div>
<% end %>
